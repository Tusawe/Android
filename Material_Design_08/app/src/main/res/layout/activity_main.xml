<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/colorAccent"
        app:cardUseCompatPadding="true"
        app:cardCornerRadius="6dp"
        app:cardElevation="10dp">

        <LinearLayout
            android:id="@+id/linear1"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <Toolbar
                android:id="@+id/toolBar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="#008DFF"
                android:popupTheme="@style/Theme.AppCompat.Light"
                android:subtitleTextAppearance="@style/subtitulo"
                android:theme="@style/tarjeta"
                android:titleTextAppearance="@style/titulo"
                android:title="El rapto de las Sabinas"
                android:subtitle="Jacques-Louis David.">
            </Toolbar>

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/cuadro2"
            android:scaleType="fitStart"/>

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>